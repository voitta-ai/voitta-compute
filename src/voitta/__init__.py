"""Briefcase entry-point package for Voitta.

Briefcase wants a Python package matching the app slug to drive ``__main__``.
This package contains *no logic* — it just delegates to ``app.desktop_launcher``
so all the real code stays in ``app/`` exactly as it lives during development.
"""
