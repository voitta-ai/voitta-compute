/**
 * Voitta Enterprise portal plugin — frontend primitives.
 *
 * Placeholder. Register primitives here with ``registerPrimitive()``
 * (imported from the core bridge) when you add browser-side tools.
 *
 * Example skeleton:
 *
 *   import { registerPrimitive } from "../../src/lib/bridge";
 *
 *   registerPrimitive("enterprise_get_page_context", async (_args) => {
 *     return {
 *       ok: true,
 *       url: window.location.href,
 *       title: document.title,
 *     };
 *   });
 *
 * The host-gating check (runs only on enterprise.voitta.ai) is applied
 * automatically by the core bridge based on the manifest host_patterns.
 */
